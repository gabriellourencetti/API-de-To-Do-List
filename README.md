# API-de-To-Do-List

express → cria o servidor e define as rotas (os "endereços" da API)
mysql2 → conecta o Node.js ao banco MySQL
cors → permite que o Next.js (outra origem) fale com a API. Sem isso, o navegador bloqueia a requisição.